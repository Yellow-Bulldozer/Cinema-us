{
  "name": "cinema-us-root",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "postinstall": "cd backend && npx prisma generate",
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend && cd backend && npx prisma generate",
    "start": "node backend/init-db.js && node backend/server.js"
  }
}
