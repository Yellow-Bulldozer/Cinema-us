{
  "name": "cinema-us-root",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend && npx --prefix backend prisma generate",
    "start": "npx --prefix backend prisma db push && node backend/server.js"
  }
}
